// const add = function (x, y) {
//     return x + y;
// }

// const add = (x, y) => {
//     return x + y;
// }

const square = (num) => {
    return num * num;
}

// const random = () => {
//     return Math.random();
// }

const random = () => (
    Math.random()
);

// addの省略
// const add = (x, y) => (
//     x + y
// );

// 一行の場合はさらに省略
const add = (x, y) => x + y;